# 视频号批量抓取实现笔记

## 问题背景

视频号（channels.weixin.qq.com）在个人主页页面无法批量抓取视频，只有点击进入某个视频后才能抓取到。进入视频后，右侧"更多推荐"中的视频也能被抓取。

## 原因分析

### 现有机制

QqPlugin 采用 **JS 注入 + 回调** 的拦截模式：

1. MITM 代理拦截 `channels.weixin.qq.com` 的 HTML 响应，给 JS 文件加版本号实现缓存失效
2. 拦截 `res.wx.qq.com` 上的 `virtual_svg-icons-register.publish.js`（视频号核心 JS bundle）
3. 在该 JS 中注入两个钩子：
   - `get media()` — 当视频对象被渲染时，POST 其 `objectDesc` 到代理回调端点
   - `finderGetCommentDetail()` — 获取视频详情时，POST 返回的 `objectDesc`
4. 代理拦截这些 POST 请求，从 `objectDesc` 中提取视频 URL、封面、文件大小、解密密钥等信息

### 为什么主页列表不生效

- 主页路径是 `/web/pages/profile`，但原先只拦截了 `/web/pages/feed` 和 `/web/pages/home`
- 主页的视频列表只显示缩略图，不会触发 `get media()` getter
- 主页加载视频列表使用的是 `finderUserPage` API，这个函数没有被 hook

### 关键参考

参考项目 [ltaoo/wx_channels_download](https://github.com/ltaoo/wx_channels_download) 的实现：

- 该项目对 `virtual_svg-icons-register.publish.js` 注入了大量钩子，包括：
  - `finderInit` — 初始化
  - `finderPcFlow` — PC 端信息流
  - `finderGetCommentDetail` — 视频详情
  - `finderGetRecommend` — 推荐列表
  - **`finderUserPage`** — 用户主页视频列表（关键）
  - `finderLiveUserPage` — 直播列表
  - `finderPCSearch` / `finderSearch` — 搜索
  - `finderGetInteractionedFeedList` — 互动列表
- 该项目在 `/web/pages/profile` 页面注入独立的 `profile.js`，通过调用被 hook 的 `finderUserPage` 实现分页获取所有视频

## 解决方案

只需修改 `core/plugins/plugin.qq.com.go`，做三处改动：

### 1. 添加 `finderUserPage` 正则

```go
var qqUserPageRegex = regexp.MustCompile(`async\s*finderUserPage\((\w+)\)\s*\{return(.*?)\s*}\s*async`)
```

### 2. 扩展 HTML 拦截路径

在 `channels.weixin.qq.com` 的 HTML 响应拦截中，增加 `/web/pages/profile`：

```go
if strings.HasSuffix(host, "channels.weixin.qq.com") &&
    (strings.Contains(Path, "/web/pages/feed") ||
     strings.Contains(Path, "/web/pages/home") ||
     strings.Contains(Path, "/web/pages/profile")) {  // 新增
    return p.replaceWxJsContent(resp, ".js\"", ".js?v="+p.v()+"\"")
}
```

### 3. 注入 `finderUserPage` 钩子（含互动数据）

在 `virtual_svg-icons-register.publish.js` 的注入逻辑中，增加对 `finderUserPage` 的 hook。

**关键发现**：`likeCount`、`favCount`、`forwardCount`、`commentCount` 等互动数据不在 `objectDesc` 内部，而是在 feed item 对象上（与 `objectDesc` 同级）。因此需要用 `Object.assign` 将互动数据合并到 payload 中：

```go
newBody = qqUserPageRegex.ReplaceAllString(newBody, `
    async finderUserPage($1) {
        var res = await$2;
        if (res?.data?.object) {
            res.data.object.forEach(function(item) {
                if (item?.objectDesc) {
                    var payload = Object.assign({}, item.objectDesc);
                    if (item.likeCount !== undefined) payload._likeCount = item.likeCount;
                    if (item.favCount !== undefined) payload._favCount = item.favCount;
                    if (item.forwardCount !== undefined) payload._forwardCount = item.forwardCount;
                    if (item.commentCount !== undefined) payload._commentCount = item.commentCount;
                    fetch("https://wxapp.tc.qq.com/res-downloader/wechat?type=1", {
                      method: "POST",
                      mode: "no-cors",
                      body: JSON.stringify(payload),
                    });
                }
            });
        }
        return res;
    }async
`)
```

**原理**：`finderUserPage` 是视频号前端在用户主页加载/滚动时调用的分页 API。返回值 `res.data.object` 是一个视频数组，每个视频都有 `objectDesc` 字段，格式与 `get media()` 中的完全一致。遍历数组将每个视频的 `objectDesc` POST 到已有的回调端点，复用现有的 `handleMedia` 逻辑即可。

## 数据流

```
用户打开视频号主页 (channels.weixin.qq.com/web/pages/profile?username=xxx)
    │
    ▼
代理拦截 HTML 响应，替换 .js 引用加版本号（缓存失效）  ← 新增 /web/pages/profile 路径
    │
    ▼
浏览器加载 virtual_svg-icons-register.publish.js?v=xxx
    │
    ▼
代理拦截该 JS，注入三个钩子：
  - get media()               → POST objectDesc (单个视频渲染时)
  - finderGetCommentDetail()  → POST objectDesc (视频详情加载时)
  - finderUserPage()          → POST 每个视频的 objectDesc (主页列表加载时)  ← 新增
    │
    ▼
用户滚动主页 → 前端调用 finderUserPage 分页加载
    │
    ▼
注入的 JS 遍历 res.data.object，逐个 POST objectDesc
    │
    ▼
代理 OnRequest 拦截 POST → handleMedia 解析 → bridge.Send("newResources", ...)
    │
    ▼
前端收到 newResources 事件，展示在资源列表中
```

## objectDesc 数据结构（关键字段）

```json
{
  "description": "视频标题/描述",
  "media": [{
    "url": "https://finder.video.qq.com/...",
    "urlToken": "&token=xxx&sign=xxx...",
    "coverUrl": "封面图 URL",
    "fileSize": 85219366,
    "decodeKey": "1336046889",
    "mediaType": 4,
    "videoPlayLen": 55,
    "width": 1080,
    "height": 1920,
    "spec": [
      {"fileFormat": "xWT111", "codingFormat": "h264", "width": 1080, "height": 1920},
      {"fileFormat": "xWT113", "codingFormat": "h264", "width": 720, "height": 1280}
    ]
  }],
  "mediaType": 4
}
```

- `mediaType: 4` = 视频，`mediaType: 9` = 图片，`mediaType: 2` = 图文
- `url + urlToken` = 完整下载地址
- `decodeKey` = 视频解密密钥（视频号视频是加密的）
- `spec` 数组 = 不同清晰度规格

## 点赞数（互动数据）获取

### 问题

最初实现中，`finderUserPage` hook 只发送 `item.objectDesc`，前端点赞数列始终为空。

### 根因分析（三个问题）

#### 1. 互动数据不在 `objectDesc` 内部

`finderUserPage` 返回的 feed item 数据结构：

```json
{
  "id": "...",
  "objectDesc": { "media": [...], "description": "..." },
  "likeCount": 1234,
  "favCount": 56,
  "forwardCount": 78,
  "commentCount": 90,
  "contact": { "nickname": "...", "headUrl": "..." }
}
```

`likeCount` 等字段在 item 对象上，与 `objectDesc` 同级，不在 `objectDesc` 内部。原来的 `JSON.stringify(item.objectDesc)` 丢失了所有互动数据。

**解决**：JS hook 中用 `Object.assign({}, item.objectDesc)` 复制 objectDesc，然后将 `item.likeCount` 等以 `_likeCount`（带下划线前缀避免字段冲突）注入到 payload 中。

#### 2. JS 缓存破坏机制失效

`v()` 返回静态版本号 `"3.1.3"`，浏览器首次加载修改后的 JS 后缓存了 `xxx.js?v=3.1.3`，后续访问不再请求代理，注入的 hook 代码无法更新。

**现象**：`channels.weixin.qq.com` 的 HTML 被代理拦截了（路径匹配成功），但 `res.wx.qq.com` 的 JS 文件请求数为零——浏览器直接使用了缓存。

**解决**：添加 `qqStartTime`（进程启动时的 Unix 时间戳），`v()` 返回 `版本号.时间戳`（如 `"3.1.3.1738569123"`），每次重启 app 都强制浏览器重新加载 JS。

```go
var qqStartTime = fmt.Sprintf("%d", time.Now().Unix())

func (p *QqPlugin) v() string {
    return p.bridge.GetVersion() + "." + qqStartTime
}
```

**参考**：`ltaoo/wx_channels_download` 也使用类似的版本号缓存破坏，但用的是静态构建版本号。他们的场景不同（每次用户手动更新版本），而我们需要每次重启都刷新。

#### 3. `get media()` 与 `finderUserPage` 的竞态条件

两个 hook 都会发送同一个视频的数据到代理：
- `get media()` — 视频渲染时触发，只发送 `objectDesc`（无互动数据）
- `finderUserPage` — 列表加载时触发，发送 `objectDesc` + 互动数据

由于 `handleMedia` 使用 `MediaIsMarked(urlSign)` 去重，先到达的创建资源，后到达的被丢弃。如果 `get media()` 先到（无互动数据），`finderUserPage` 的数据（有互动数据）就被丢弃了。

**解决**：添加 `tryUpdateEngagement` 方法——当 URL 已标记但传入数据包含互动字段时，发送 `updateResourceMeta` 事件更新前端已有资源的 `OtherData`：

```go
// Go 后端
func (p *QqPlugin) handleMedia(body []byte) {
    // ...
    urlSign := shared.Md5(rawUrl)
    if p.bridge.MediaIsMarked(urlSign) {
        p.tryUpdateEngagement(urlSign, result)  // 不再直接 return
        return
    }
    // ... 正常创建资源流程
}

func (p *QqPlugin) tryUpdateEngagement(urlSign string, result map[string]interface{}) {
    update := map[string]string{}
    if likeCount, ok := result["_likeCount"].(float64); ok {
        update["likeCount"] = strconv.FormatFloat(likeCount, 'f', 0, 64)
    }
    // ... favCount, forwardCount, commentCount 同理
    if len(update) > 0 {
        p.bridge.Send("updateResourceMeta", map[string]interface{}{
            "UrlSign":   urlSign,
            "OtherData": update,
        })
    }
}
```

```typescript
// 前端 index.vue
eventStore.addHandle({
  type: "updateResourceMeta",
  event: (res: { UrlSign: string, OtherData: Record<string, string> }) => {
    const item = data.value.find(i => i.UrlSign === res.UrlSign)
    if (item && res.OtherData) {
      if (!item.OtherData) item.OtherData = {}
      Object.assign(item.OtherData, res.OtherData)
      cacheData()
    }
  }
})
```

### finderUserPage 返回的互动字段

| 字段 | 含义 | Go 中存储键 |
|------|------|------------|
| `likeCount` | 点赞数 | `OtherData["likeCount"]` |
| `favCount` | 收藏数 | `OtherData["favCount"]` |
| `forwardCount` | 转发数 | `OtherData["forwardCount"]` |
| `commentCount` | 评论数 | `OtherData["commentCount"]` |

### 调试经验

- Wails 生产构建的 app 无法看到 `fmt.Printf` 输出，需要用文件日志（写入 `/tmp/qq-debug.log`）调试
- `wails dev` 开发模式下可以在终端看到 Go 的标准输出
- 排查 JS 注入问题时，关键是检查 `res.wx.qq.com` 的 JS 请求是否到达代理——如果浏览器缓存了旧 JS，代理日志中不会有任何 `res.wx.qq.com` 记录

## 注意事项

- 视频号的视频是加密传输的，下载后需要用 `decodeKey` 解密
- `finderUserPage` 每次返回约 15 个视频，浏览器滚动时自动分页
- 用户只需在主页正常滚动，视频就会自动被捕获到资源列表中，无需额外操作
- 回调使用 `type=1`（与 `get media()` 相同），因为数据格式一致，复用同一处理逻辑
- 互动数据通过 `_likeCount` 等带下划线前缀的字段传输，避免与 `objectDesc` 原有字段冲突
- 缓存破坏版本号每次 app 启动都会变化，确保 JS 注入代码始终最新
