# 小红书资源下载操作流程

## 概述

res-downloader 通过本地 HTTP 代理拦截浏览器与小红书服务器之间的网络请求，自动解析并提取图片、视频等资源。用户只需在浏览器中正常浏览小红书页面，软件即可自动捕获资源。

## 支持的资源类型

| 资源类型 | 来源场景 | 说明 |
|---------|---------|------|
| 图片 | 浏览用户主页 / 打开笔记详情 | 支持轮播图（多图笔记），格式包括 jpg、png、webp |
| 视频 | 打开视频笔记详情 | 自动选择最优编码（H.264 > H.265 > AV1），格式为 mp4 |

## 操作步骤

### 1. 启动代理

1. 打开 res-downloader
2. 点击首页左上角 **"启动代理"** 按钮
3. 根据系统提示 **允许安装证书文件** 并 **允许网络访问**
4. 确认系统代理已设置为 `127.0.0.1:8899`

### 2. 浏览小红书页面

1. 打开浏览器，访问 [小红书](https://www.xiaohongshu.com/)
2. 进入目标用户的个人主页，如：`https://www.xiaohongshu.com/user/profile/xxxxx`
3. **滚动页面**，让浏览器加载更多笔记内容
4. 如需获取完整图片或视频，**点击进入笔记详情页**

> **说明：** 小红书资源通过被动拦截方式获取。软件会在浏览器请求 `edith.xiaohongshu.com` 相关接口时自动解析数据，无需手动操作。

### 3. 查看捕获的资源

1. 切换回 res-downloader 窗口
2. 资源列表会实时显示已捕获的内容
3. 每条资源包含以下信息：
   - 域名（xiaohongshu.com）
   - 类型（图片 / 视频）
   - 描述（笔记标题）
   - 预览图
   - 点赞数

### 4. 筛选资源

- 使用顶部类型筛选按钮（图片 / 视频）过滤资源
- 使用搜索框按描述或 URL 搜索

### 5. 下载资源

**单个下载：** 右键点击资源，选择下载

**批量下载：**
1. 勾选需要下载的资源（或全选）
2. 点击 **"批量下载"** 按钮
3. 下载路径可在设置中配置
4. 下载状态会实时更新（等待 / 进行中 / 完成 / 失败）

### 6. 导出资源信息

- 支持将资源列表导出为 URL 列表或 Excel 文件
- 可用于后续批量处理或备份记录

## 工作原理

```
浏览器访问小红书
    ↓
本地代理(127.0.0.1:8899)拦截 HTTPS 请求
    ↓
识别 edith.xiaohongshu.com 接口响应
    ├── /api/sns/web/v1/user_posted  → 用户主页笔记列表（封面图）
    └── /api/sns/web/v1/feed         → 笔记详情（完整图片/视频）
    ↓
解析 JSON，提取资源 URL 及元数据
    ↓
去重后推送至前端资源列表
    ↓
用户选择并下载
```

## 注意事项

- **必须启动代理**才能拦截资源，浏览前请确认代理已开启
- **滚动用户主页**可批量获取笔记封面图；**点击进入笔记详情**可获取完整图片和视频
- 视频资源仅在打开笔记详情页时才能获取，仅浏览主页无法获取视频下载链接
- 图片笔记在主页浏览时只获取封面，进入详情页可获取全部轮播图
- 软件会自动去重，同一资源不会重复添加
- 关闭软件前建议先 **关闭代理**，否则可能导致无法正常上网（可手动关闭系统代理）
- 提取的资源为小红书 CDN 直链，可直接下载，不依赖代理

## 常见问题

**Q: 浏览了很多笔记但没有捕获到资源？**
A: 请检查代理是否已启动，系统代理是否正确设置为 `127.0.0.1:8899`。

**Q: 只看到图片没有视频？**
A: 视频资源需要点击进入笔记详情页才能获取，仅浏览主页只能获取封面图片。

**Q: 图片笔记只显示一张图片？**
A: 多图笔记在主页浏览时只获取封面图。进入笔记详情页可获取全部图片。

**Q: 关闭软件后无法上网？**
A: 手动关闭系统代理设置即可恢复。
