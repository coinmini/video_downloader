# 小红书资源下载操作流程

## 概述

res-downloader 支持从小红书批量获取图片和视频资源。通过本地代理捕获浏览器的 Cookie 和签名信息后，可主动调用小红书 API 批量获取用户主页下的所有笔记资源（包括视频）。

## 支持的资源类型

| 资源类型 | 说明 |
|---------|------|
| 图片 | 笔记封面图及轮播图，格式包括 jpg、png、webp |
| 视频 | 视频笔记，自动选择最优编码（H.264 > H.265 > AV1），格式为 mp4 |

## 操作步骤

### 1. 启动代理

1. 打开 res-downloader
2. 点击首页左上角 **"启动代理"** 按钮
3. 根据系统提示 **允许安装证书文件** 并 **允许网络访问**
4. 确认系统代理已设置为 `127.0.0.1:8899`

### 2. 浏览小红书用户主页（捕获 Cookie 和签名）

1. 打开浏览器，访问 [小红书](https://www.xiaohongshu.com/)
2. 进入目标用户的个人主页，如：`https://www.xiaohongshu.com/user/profile/xxxxx`
3. 等待页面加载完成（软件需要从浏览器请求中捕获 Cookie 和 API 签名 header）

> **重要：** 此步骤是为了让代理捕获浏览器发出的 Cookie 和签名信息（X-s、X-t 等），后续批量获取时需要使用这些信息。

### 3. 批量获取笔记资源

1. 切换回 res-downloader 窗口
2. 点击 **"批量获取"** 按钮
3. 粘贴用户主页链接，如：`https://www.xiaohongshu.com/user/profile/xxxxx`
4. 点击 **"开始获取"**
5. 软件会自动分页获取该用户的所有笔记：
   - 图片笔记：直接获取封面图
   - 视频笔记：自动调用详情接口获取视频下载链接
6. 获取进度会实时显示在对话框中
7. 如需中止，点击 **"取消获取"**

### 4. 查看和筛选资源

1. 获取到的资源会实时显示在主列表中
2. 每条资源包含：域名、类型（图片/视频）、描述、预览图、点赞数
3. 使用顶部类型筛选按钮（图片/视频）过滤资源
4. 使用搜索框按描述或 URL 搜索

### 5. 下载资源

**单个下载：** 右键点击资源，选择下载

**批量下载：**
1. 勾选需要下载的资源（或全选）
2. 点击 **"批量下载"** 按钮
3. 下载路径可在设置中配置
4. 下载状态会实时更新（等待 / 进行中 / 完成 / 失败）

### 6. 导出资源信息

- 支持将资源列表导出为 URL 列表或 Excel 文件
- 可用于后续批量处理或备份记录

## 工作原理

```
1. 启动代理，浏览器访问小红书用户主页
   → 代理捕获 Cookie 和签名 headers (X-s, X-t, X-s-common)
       ↓
2. 用户粘贴主页 URL，点击"开始获取"
       ↓
3. 后端主动调用 user_posted API（分页）
   → 获取笔记列表（note_id, type, cover, title）
       ↓
4. 对每个视频笔记，调用 feed API
   → 获取真实视频下载链接（从 media.stream 中提取）
       ↓
5. 图片和视频资源推送至前端列表
       ↓
6. 用户选择并下载
```

## 注意事项

- **必须先浏览用户主页**让代理捕获 Cookie 和签名信息，否则批量获取会失败
- 如果遇到限流（API 返回错误），软件会提示刷新浏览器页面以获取新的 Cookie/签名
- 软件会自动去重，同一资源不会重复添加
- 关闭软件前建议先 **关闭代理**，否则可能导致无法正常上网（可手动关闭系统代理）
- 提取的资源为小红书 CDN 直链，可直接下载，不依赖代理

## 常见问题

**Q: 点击开始获取后提示"未检测到Cookie"？**
A: 请先开启代理，然后在浏览器中打开小红书用户主页，让代理捕获 Cookie。

**Q: 获取过程中提示被限流？**
A: 在浏览器中刷新小红书页面，软件检测到新的 Cookie 后会自动继续获取。

**Q: 关闭软件后无法上网？**
A: 手动关闭系统代理设置即可恢复。
